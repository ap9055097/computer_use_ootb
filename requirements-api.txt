# API-only requirements for Windows packaging (PyInstaller)
# Excludes ML dependencies (torch, transformers, etc.)

# From dev-requirements.txt (with version constraints)
pyautogui==0.9.54
anthropic[bedrock,vertex]>=0.49.0
jsonschema==4.22.0
boto3>=1.28.57
google-auth<3,>=2
gradio>=5.6.0,<6.0.0
screeninfo
uiautomation
numpy<2
pydantic==2.9.2

# From dev-requirements-addon.txt
pynput
pyperclip          # Clipboard paste for Thai/Unicode typing (avoids pynput corruption)
imagehash
Pillow
google-generativeai

# Required by gradio_client for share functionality
huggingface_hub>=0.26.0,<1.0

# Gradio dependencies (ensure they're included for PyInstaller)
httpx
websockets
aiofiles
python-multipart
fsspec
tomlkit
typer
rich
shellingham

# Additional for Windows packaging
win10toast         # Windows notifications for public URL display
